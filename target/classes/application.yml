#配置tomcat启动端口
server:
  port: 8081

#配置数据库连接
spring:
  datasource:
    url: jdbc:mysql://localhost:3306/mybaties?useUnicode=true&characterEncoding=UTF-8&useSSL=false
    driver-class-name: com.mysql.jdbc.Driver
    username: root
    password: 123456
#    配置视图解析器
  mvc:
    view:
      prefix: /
      suffix: .html